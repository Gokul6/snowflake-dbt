measures:
  - name: total_sales_amount
    description: "Total sales amount across all orders"
    expr: total_amount
    agg: sum
metrics:
  - name: total_sales
    description: "Total sales amount across all orders"
    type: simple
    label: "Total Sales"
    type_params:
      measure:
        name: total_sales_amount      
