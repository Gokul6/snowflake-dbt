# models/metrics.yml

version: 2
metrics:
  - name: total_orders
    description: "Counts the total number of completed orders"
    model: ref('orders') # Model to base this metric on
    type: count
    sql: "order_id"  # Field or expression to count
    dimensions:
      - customer_id
      - order_date